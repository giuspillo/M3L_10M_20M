### Dataset Processing

In this folder, we release the source code we used to process the original datasets into the MMRec format, with the enriched multimodal features. This is done by using two distinct Python notebooks, that can be found in the `ml10m` and `ml20m` folders.